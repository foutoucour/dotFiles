
# sorts by size 
ls -l | grep ^- | sort -nr -k 5

# sorts by date
ls -l | grep ^- | sort -nr -k 8


# numbers of file in folder
ls | wc -l


# lists processes
top 

# kills process
kill (-9) ID


# uses maya without DEVTOOLS setup
env DEVTOOLS=/garbage maya 



# compares 2 folder (dirA and dirB) excluding 'Thumbs' and 'DS_Store' files
diff -qr dirA dirB | grep -v -e 'DS_Store' -e 'Thumbs' | sort


grep -lir "some text" *